# ──────────────────────────────────────────────────────────────────────────────
# ASTRA-v2 Python dependencies
# ──────────────────────────────────────────────────────────────────────────────

# ── Core browser automation
playwright>=1.44.0
pytest-playwright>=0.4.4

# ── Test framework
pytest>=8.2.0
pytest-xdist>=3.5.0        # parallelism (-n auto)
pytest-rerunfailures>=14.0 # flaky test retry

# ── Allure reporting
allure-pytest>=2.13.5
allure-python-commons>=2.13.5

# ── API / HTTP
httpx>=0.27.0
websockets>=12.0            # WebSocket client (optional — imported lazily)

# ── Configuration
python-dotenv>=1.0.0

# ── Logging
colorlog>=6.8.2

# ── ML — self-healing model training & inference
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.4.0
skl2onnx>=1.16.0            # sklearn → ONNX export
onnxruntime>=1.17.0         # CPU-only ONNX inference

# ── YAML (OpenAPI spec parsing — optional)
pyyaml>=6.0.1

# ── Notifications
# httpx already covers Slack/Teams webhooks (HTTP POST)
# smtplib is stdlib (email)

# ── Development / linting (optional, not required at runtime)
# ruff>=0.4.0
# mypy>=1.9.0
