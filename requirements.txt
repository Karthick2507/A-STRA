# ──────────────────────────────────────────────────────────────────────────────
# PRISM dependencies — shadow coding + self healing only
# ──────────────────────────────────────────────────────────────────────────────

# Browser automation (shadow coding wraps playwright codegen)
playwright>=1.44.0
pytest-playwright>=0.4.4

# Test framework (used for plugin's own test suite + standalone runs)
pytest>=8.2.0

# Configuration
python-dotenv>=1.0.0

# Logging
colorlog>=6.8.2

# ML — self-healing model training & inference
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.4.0
skl2onnx>=1.16.0       # sklearn → ONNX export
onnxruntime>=1.17.0    # CPU-only ONNX inference

# Folder scanner (Option C) — file content classification
pyyaml>=6.0.1          # YAML data files
openpyxl>=3.1.0        # XLSX data files
python-docx>=1.1.0     # Word doc data files
python-pptx>=0.6.23    # PPT data files
